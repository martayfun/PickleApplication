// Root namespace
var Sf = {
    Core: {}
};

// Each object must be extended with the Sf.Core 
$.extend(Sf.Core,
    {
        App: {}
    });

// Each object must be extended with the Sf.Core.App
$.extend(Sf.Core.App,
    {
        B2C: {}
    });

// Each object must be extended with the Sf.Core.App
$.extend(Sf.Core.App,
    {
        Cms: {}
    });
